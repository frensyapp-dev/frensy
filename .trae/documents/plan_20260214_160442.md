Je propose de procéder par étapes pour sécuriser et optimiser l'application, en commençant par la fonctionnalité critique des **Cadeaux Journaliers** qui présente actuellement une faille de sécurité majeure (modification du solde côté client).

### Phase 1 : Sécurisation et Réparation des Cadeaux Journaliers
**Objectif :** Rendre le système de récompense robuste, sécurisé et cohérent entre le client et le serveur.

1.  **Correction de la Faille de Sécurité** :
    *   Modifier `app/store.tsx` pour qu'il n'écrive plus jamais directement dans Firestore pour les récompenses.
    *   Connecter l'interface à la Cloud Function `claimDailyReward` existante (mais inutilisée).
2.  **Harmonisation de la Logique** :
    *   Supprimer la logique de calcul de date/streak côté client (qui dépend de l'heure du téléphone) pour se fier entièrement au serveur via la Cloud Function.
    *   Centraliser la configuration des récompenses (`REWARDS`) pour éviter les duplications entre le front et le back.
3.  **Amélioration de l'Expérience Utilisateur (UX)** :
    *   Ajouter un état de chargement visuel pendant l'appel réseau sécurisé.
    *   Gérer proprement les erreurs (ex: tentative de double réclamation).

### Phase 2 : Analyse Globale et Optimisation (Sur la base de votre demande)
Une fois la fonctionnalité "Cadeaux" réparée, je procéderai à l'analyse des autres points critiques que j'ai identifiés :

1.  **Sécurité Globale** :
    *   Vérifier d'autres endroits où `applyUserUpdates` est utilisé (ex: achat de boosts, invits) car ils sont probablement vulnérables aux mêmes attaques.
2.  **Performance (Page Home)** :
    *   Optimiser `home.tsx` : La logique de filtrage des utilisateurs proches (`nearbyAll`) est recalculée trop souvent et peut ralentir l'interface. Je proposerai une mémorisation (`useMemo`) plus efficace.
3.  **Qualité du Code** :
    *   Nettoyer les imports inutilisés et les `console.log`.
    *   Typage : Remplacer les `any` par des types stricts (ex: dans `store.tsx` et `functions/src/index.ts`) pour éviter les crashs silencieux.

Je suis prêt à commencer par la **Phase 1** immédiatement. Confirmez-vous ce plan d'action ?